enum Routes {
  HOME = '/',
  EPISODES = '/episode',
  CHARACTERS = '/character',
  LOCATIONS = '/location',
  EPISODE = '/episode/:id',
  CHARACTER = '/character/:id',
  LOCATION = '/location/:id',
}

export default Routes;
